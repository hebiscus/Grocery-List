extends layout 

block content
    h1= title 
    div 
        each category in category_list
            div 
                a(href=category.url) #{category.name}
        a(href="categories/category/create")
            button Create Category